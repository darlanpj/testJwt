server:
  port: 8080

spring:
  application:
    name: jwt-validator

springdoc:
  api-docs:
    path=/api-docs: /api-docs

#prometeus
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    metrics:
      enabled: false
    prometheus:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true


